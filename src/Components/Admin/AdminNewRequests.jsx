import React from "react";
import { useState, useEffect } from "react";
import axios from "axios";
/* this will have all new requests also checks if they are new/like in submitted to show popup in main admins creen*/
export default function AdminNewRequests({ newRequests }) {
  console.log(newRequests);
  return <div>AdminNewRequests</div>;
}
